propTypes: {
  choiches: PropTypes.array.isRequired,
  heading: PropTypes.string,
  headingStyle: PropTypes.string,
  inputStyle: PropTypes.string,
  name: PropTypes.string.isRequired,
  query: PropTypes.object
}
